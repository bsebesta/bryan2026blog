---
title: Library
slug: library
layout: library
# Books and films both export as `log` (config.yaml type_by_folder). This is
# the reading/watching log — the section formerly called Learning. Active
# study (SRS) and surveys now live on the separate Learning page.
hub_types:
  - log
build:
  list: never
---

Books and films.
